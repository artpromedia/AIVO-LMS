import { PageSkeleton } from "@/components/ui/page-skeleton";

/** Sprint C-05: loading state for the review & correct screen. */
export default function Loading() {
  return <PageSkeleton variant="detail" />;
}
