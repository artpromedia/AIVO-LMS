fastapi>=0.136.1
uvicorn[standard]==0.34.3
pydantic>=2.11.4,<3
python-dotenv>=1.0.1,<2
# RS256 access-token verification (shares identity-svc's JWT_PUBLIC_KEY).
pyjwt[crypto]>=2.10.1,<3
# Postgres-backed catalogue store (CURRICULUM_PERSISTENCE=postgres, Sprint 4).
psycopg[binary]>=3.1,<4
