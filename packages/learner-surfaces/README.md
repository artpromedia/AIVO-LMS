# @aivo/learner-surfaces

Media surfaces (`VideoSurface`, `AudioSurface`) use **hls.js** for HLS playback fallback when native HLS is unavailable. Native playback is still used for MP4/MP3/AAC and browsers with built-in HLS support.
