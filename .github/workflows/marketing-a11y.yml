name: Marketing Accessibility Audit

on:
  pull_request:
    paths:
      - "apps/marketing/**"
      - "packages/brand/**"
      - "packages/ui/**"

permissions:
  contents: read

concurrency:
  group: marketing-a11y-${{ github.ref }}
  cancel-in-progress: true

jobs:
  accessibility:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build marketing site
        run: pnpm --filter @aivo/marketing build

      - name: Run accessibility audit
        run: |
          npx lighthouse-ci autorun --collect.url="http://localhost:4000" || true
        continue-on-error: true
